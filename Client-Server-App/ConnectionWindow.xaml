<Window x:Class="Client_Server_App.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client_Server_App"
        mc:Ignorable="d"
        Title="ConnectionWindow" Height="253" Width="500">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<!-- Connect Section -->
		<StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock Text="Connect" FontWeight="Bold" Margin="0,0,0,10" />
			<TextBlock Text="IP Address:" Margin="0,0,0,5" />
			<TextBox Width="200" Margin="0,0,0,10" />
			<TextBlock Text="Port:" Margin="0,0,0,5" />
			<TextBox Width="200" Margin="0,0,0,10" />
			<Button Content="Connect" Width="100" Margin="0,0,0,10" Click="Button_Click"/>
			<Button Content="Test Connection" Width="100" />
		</StackPanel>

		<!-- Create Section -->
		<StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock Text="Create" FontWeight="Bold" Margin="0,0,0,10" />
			<TextBlock Text="Port:" Margin="0,0,0,5" />
			<TextBox Width="200" Margin="0,0,0,10" />
			<Button Content="Create Host" Width="100" Click="HostButton_Click" />
		</StackPanel>
	</Grid>
</Window>