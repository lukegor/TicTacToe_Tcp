<Window x:Class="Client_Server_App.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connection" Height="420" Width="560"
        WindowStartupLocation="CenterScreen">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- Connect / Host panels -->
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<!-- Connect Section -->
			<StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
				<TextBlock Text="Connect" FontWeight="Bold" Margin="0,0,0,10" />
				<TextBlock Text="IP Address:" Margin="0,0,0,5" />
				<TextBox x:Name="AddressTextBox" Width="200" Margin="0,0,0,10" Text="127.0.0.1" />
				<TextBlock Text="Port:" Margin="0,0,0,5" />
				<TextBox x:Name="PortTextBox" Width="200" Margin="0,0,0,10" Text="1111" />
				<Button x:Name="ConnectButton" Content="Connect" Width="100" Click="ConnectButton_Click" />
			</StackPanel>

			<!-- Host Section -->
			<StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top">
				<TextBlock Text="Host" FontWeight="Bold" Margin="0,0,0,10" />
				<TextBlock Text="Port:" Margin="0,0,0,5" />
				<TextBox x:Name="HostPortTextBox" Width="200" Margin="0,0,0,10" Text="1111" />
				<Button x:Name="HostButton" Content="Create Host" Width="100" Click="HostButton_Click" />
			</StackPanel>
		</Grid>

		<!-- Message log -->
		<TextBox x:Name="OutputTextBox"
				 Grid.Row="1"
				 Margin="0,12,0,0"
				 IsReadOnly="True"
				 IsReadOnlyCaretVisible="False"
				 FontFamily="Consolas"
				 TextWrapping="Wrap"
				 VerticalScrollBarVisibility="Auto"
				 VerticalContentAlignment="Top" />
	</Grid>
</Window>
