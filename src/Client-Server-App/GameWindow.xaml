<Window x:Class="ClientServer.App.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding TitleText}" Height="450" Width="440"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary Source="Themes/Shared.xaml" />
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Grid.Row="0" Text="{Binding StatusText}"
                   HorizontalAlignment="Center"
                   FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,10" />

        <ItemsControl x:Name="BoardGrid" Grid.Row="1" ItemsSource="{Binding Cells}" Focusable="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button FontSize="32" FontWeight="Bold" Margin="2"
                            Content="{Binding Mark}"
                            IsEnabled="{Binding IsEnabled}"
                            Command="{Binding DataContext.MoveCommand,
                                      RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding CellIndex}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                        <Setter Property="Background" Value="LightGoldenrodYellow" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button x:Name="RematchButton" Grid.Row="3"
                Content="{Binding RematchLabel}"
                IsEnabled="{Binding RematchEnabled}"
                Command="{Binding OfferRematchCommand}" Width="160" Margin="0,10,0,0" />

        <Button x:Name="LeaveButton" Grid.Row="3" Content="Leave" Width="80"
                HorizontalAlignment="Right" Margin="0,10,0,0"
                Command="{Binding LeaveCommand}" />
    </Grid>
</Window>
